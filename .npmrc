# Force npm to respect package overrides and use exact versions
fund=false
audit=false
package-lock=true
save-exact=true
engine-strict=true
legacy-peer-deps=false

# Ensure overrides are respected in CI environments
prefer-online=true
cache-max=0
